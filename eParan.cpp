#include<bits/stdc++.h>

bool isValid(string str){
	stack<char> st;
	for(int i=0;str[i]!=NULL;i++){
		if(st.top()==st){
			
		}
	}
	
}

int main(){
	cout<<isValid("[]][");
	cout<<isValid("()[]{}");
	return 0;
}
